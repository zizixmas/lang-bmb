-- Simple contracts (parsing only in v0.1)
fn abs_contract(x: i64) -> i64
  pre true
  post ret >= 0
= if x >= 0 then x else 0 - x;

fn positive_add(a: i64, b: i64) -> i64
  pre a > 0 and b > 0
  post ret > a and ret > b
= a + b;
