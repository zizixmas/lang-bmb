// Test generics at runtime
// v0.15 integration test

// Non-generic version
fn identity_i64(x: i64) -> i64 = x;

// Generic version
fn identity<T>(x: T) -> T = x;

// Test generic struct
struct Container<T> {
    value: T,
}

fn main() -> i64 = identity_i64(42) + identity(100);
