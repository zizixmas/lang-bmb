// Module Header System Test (v0.31: RFC-0002)
// Tests the module header syntax with all optional fields

module math.arithmetic
  version "1.0.0"
  summary "Basic integer arithmetic operations"
  exports add, subtract, multiply
  depends
    core.types (i64)
===

// Simple addition function
fn add(a: i64, b: i64) -> i64 = a + b;

// Subtraction function
fn subtract(a: i64, b: i64) -> i64 = a - b;

// Multiplication function
fn multiply(a: i64, b: i64) -> i64 = a * b;
