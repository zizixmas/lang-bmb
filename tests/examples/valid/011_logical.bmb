// Logical operations
fn both_positive(a: i64, b: i64) -> bool =
  a > 0 and b > 0;

fn either_zero(a: i64, b: i64) -> bool =
  a == 0 or b == 0;

fn is_in_range(x: i64, lo: i64, hi: i64) -> bool =
  x >= lo and x <= hi;

fn negate(b: bool) -> bool = not b;
