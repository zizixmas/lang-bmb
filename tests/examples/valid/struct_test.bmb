// Test file for struct support

struct Point {
    x: i64,
    y: i64,
}

fn make_point(x: i64, y: i64) -> Point = {
    new Point { x: x, y: y }
};

fn get_x(p: Point) -> i64 = {
    p.x
};

fn main() -> i64 = {
    let p = make_point(10, 20);
    get_x(p)
};
