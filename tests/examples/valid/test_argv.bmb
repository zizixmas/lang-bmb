// Test argv builtins (Phase 32.3.D)
// Tests arg_count() and get_arg(n) for CLI argument access

fn print_s(s: String) -> i64 =
    let x = print_str(s);
    x;

fn main() -> i64 =
    let argc = arg_count();
    let prog = get_arg(0);
    let x = println(argc);
    let y = print_s(prog);
    argc;
