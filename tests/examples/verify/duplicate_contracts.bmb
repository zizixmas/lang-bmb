// Duplicate Contract Detection Test (v0.31)
// Tests that duplicate contracts are detected and warned

fn example(x: i64) -> r: i64
  where {
    positive: r >= 0,
    // This is a duplicate of the above contract
    also_positive: r >= 0,
    different: r <= x * 2
  }
= if x >= 0 { x } else { 0 - x };
